apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ include "hertzbeat.collector" . }}-env"
  labels:
    {{- include "hertzbeat.labels" . | nindent 4 }}
data:
  IDENTITY: ""
  MANAGER_IP: "{{ include "hertzbeat.manager.host" . }}"
  MANAGER_PORT: "1158"
